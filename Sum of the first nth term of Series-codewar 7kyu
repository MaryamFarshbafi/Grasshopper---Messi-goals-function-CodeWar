function SeriesSum(n)
{
  array=[];
  for( let i=0; i<= n-1; i++){
    array.push(1/(1+3*i));
    
  }
 const sumArray= 0;
  const aumArrayAll= array.reduce(
  (previousValue, currentValue) => previousValue + currentValue,
  sumArray);
  
  return aumArrayAll.toFixed(2);

  
}
